Class- Class is a blueprint or template used to create objects.
Object- Object are real-world entities/instances of a class that contains data(Object ki info.) and methods(object ky kaam karta hai).
It is created in memory and used to perform operations.
Data(Attributes)
Methods(Behaviors)
Ex- Pen
color tip(attri.)
setcolor(),settip()(Meth.)








